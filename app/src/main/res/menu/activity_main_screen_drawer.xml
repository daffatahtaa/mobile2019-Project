<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <item android:title="Utilities">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/alarm"
                    android:icon="@drawable/alarm"
                    android:title="@string/alarm_home"
                    />
                <item
                    android:id="@+id/currency"
                    android:icon="@drawable/currency"
                    android:title="@string/currency_home"
                    />
                <item
                    android:id="@+id/temperature"
                    android:icon="@drawable/temperature"
                    android:title="@string/temperature_home"/>
                <item
                    android:id="@+id/timer"
                    android:icon="@drawable/timer"
                    android:title="@string/timer_home"/>

            </group>
        </menu>
    </item>


    <item android:title="Other">
        <menu>
            <item
                android:id="@+id/about"
                android:icon="@drawable/about"
                android:title="@string/about" />
            <item
                android:id="@+id/logout"
                android:icon="@drawable/logout"
                android:title="@string/logout" />
        </menu>
    </item>

</menu>
